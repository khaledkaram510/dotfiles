# vim:fileencoding=utf-8:foldmethod=marker

# Pass through ZSH key bindings
map ctrl+f no_op
map ctrl+g no_op
map ctrl+r no_op
map ctrl+e no_op
map ctrl+_ no_op
map ctrl+p no_op
map ctrl+n no_op

# Include original kitty.conf
include kitty.conf.original
