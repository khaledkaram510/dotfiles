-- Create a minimal plugin that doesn't depend on external modules
return {
    setup = function()
        -- This is a placeholder function that does nothing
        -- We'll implement actual functionality when we have proper access to Yazi's API
        print("Full-border plugin loaded")
    end
}
